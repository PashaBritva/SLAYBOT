const { EmbedBuilder } = require("discord.js");

module.exports = {
  name: "rules",
  description: "показывает правила сервера",
  category: "INFORMATION",
  command: { enabled: true, usage: "" },
  slashCommand: { enabled: true, ephemeral: true },

  async messageRun(message) {
    await message.safeReply({ embeds: buildRulesEmbeds() });
  },

  async interactionRun(interaction) {
    await interaction.followUp({ embeds: buildRulesEmbeds() });
  },
};

function buildRulesEmbeds() {
  const ACCENT = "#5865F2";
  const ORANGE = "#FEE75C";
  const RED = "#ED4245";
  const GREEN = "#57F287";

  return [
    // Вступление
    new EmbedBuilder()
      .setColor(ACCENT)
      .setTitle("📜 Правила сервера")
      .setDescription(
        `Добро пожаловать! Ознакомьтесь с правилами ниже.\n\n` +
          `Незнание правил **не освобождает** от ответственности.\n` +
          `Администрация оставляет за собой право изменять правила.\n\n` +
          `⚠️ — предупреждение • 🔇 — мут • 🚫 — бан`
      )
      .setFooter({ text: "Следите за обновлениями в 📰 news" }),

    // 1. Уважение
    new EmbedBuilder()
      .setColor(ACCENT)
      .setTitle("1 — Уважение")
      .setDescription(
        `**Правило:** Уважайте всех участников сервера. Оскорбления, угрозы, преследования и грубые высказывания запрещены.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — ⚠️ предупреждение\n` +
          `2-е нарушение — 🔇 мут на **3 часа**\n` +
          `Повторное — 🚫 бан на **1 день**`
      ),

    // 2. Язык
    new EmbedBuilder()
      .setColor(ACCENT)
      .setTitle("2 — Язык общения")
      .setDescription(
        `**Правило:** Основной язык сервера — **русский**. Использование других языков допускается только в соответствующих каналах.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — ⚠️ предупреждение\n` +
          `2-е нарушение — 🔇 мут на **1 час**\n` +
          `Повторное — 🔇 мут на **6 часов**`
      ),

    // 3. Запрещённые темы
    new EmbedBuilder()
      .setColor(ORANGE)
      .setTitle("3 — Запрещённые темы")
      .setDescription(
        `**Правило:** Обсуждение **политики**, **религии** и иных провокационных тем строго запрещено.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — ⚠️ предупреждение\n` +
          `2-е нарушение — 🔇 мут на **6 часов**\n` +
          `Повторное — 🚫 бан на **1 день**`
      ),

    // 4. Флуд и спам
    new EmbedBuilder()
      .setColor(ACCENT)
      .setTitle("4 — Флуд и спам")
      .setDescription(
        `**Правило:** Запрещено флудить, спамить и оффтопить в любых каналах. Поддерживайте порядок.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — ⚠️ предупреждение\n` +
          `2-е нарушение — 🔇 мут на **3 часа**\n` +
          `Повторное — 🔇 мут на **12 часов**`
      ),

    // 5. Реклама
    new EmbedBuilder()
      .setColor(ORANGE)
      .setTitle("5 — Реклама")
      .setDescription(
        `**Правило:** Реклама, самореклама и распространение сторонних ссылок запрещены без разрешения администрации.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — ⚠️ предупреждение\n` +
          `2-е нарушение — 🔇 мут на **6 часов**\n` +
          `Повторное — 🚫 бан на **1 день**`
      ),

    // 6. NSFW
    new EmbedBuilder()
      .setColor(RED)
      .setTitle("6 — NSFW контент")
      .setDescription(
        `**Правило:** Размещение контента для взрослых **(NSFW)**, включая изображения, видео и ссылки, строго запрещено.\n\n` +
          `**Наказание:**\n` +
          `1-е нарушение — 🔇 мут на **6 часов**\n` +
          `Повторное — 🚫 бан на **3 дня**`
      ),

    // 7. Безопасность
    new EmbedBuilder()
      .setColor(RED)
      .setTitle("7 — Безопасность и мошенничество")
      .setDescription(
        `**Правило:** Запрещён фишинг, мошенничество, распространение вредоносного ПО и кража личных данных.\n\n` +
          `**Наказание:** 🚫 Бан на **7 дней** без предупреждений.`
      ),

    // Заключение
    new EmbedBuilder()
      .setColor(GREEN)
      .setTitle("✅ Напоминание")
      .setDescription(
        `• Оспаривайте решения модераторов **корректно и в личных сообщениях**\n` +
          `• Сообщайте о багах бота в специальный канал\n` +
          `• Не передавайте личные данные посторонним\n\n` +
          `Соблюдайте правила и наслаждайтесь общением! 💬`
      )
      .setFooter({ text: "SLAYBOT • Правила сервера" })
      .setTimestamp(),
  ];
}
